#!/bin/bash

#this command installs localtunnel using npm, dont have npm then there will be a command that will install npm  automatically
npm -g install localtunnel
# this command checks if localtunnel successfully installed
lt
